import React from 'react';
import LiveAdvertisements from '../../home/LiveAdvertisements';

export const AdsSection: React.FC<{ config?: any }> = () => {
  return <LiveAdvertisements />;
};
export default AdsSection;
